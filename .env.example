SECRET_KEY=replace-with-a-secure-random-secret
# development, testing, or production
FLASK_ENV=development
# Optional locally. Production should use PostgreSQL.
# DATABASE_URL=postgresql://user:password@host:5432/eventid
# Enable secure cookies when the application is served over HTTPS.
SESSION_COOKIE_SECURE=false
# Use shared persistent storage such as redis://localhost:6379 in production.
RATELIMIT_STORAGE_URI=memory://
LOGIN_RATE_LIMIT=5 per minute
# Maximum request size in bytes; the default is 5 MiB.
# MAX_CONTENT_LENGTH=5242880
# Local default: instance/event_images. Production needs a persistent disk.
# UPLOAD_DIRECTORY=/var/data/event_images
LOG_LEVEL=INFO
